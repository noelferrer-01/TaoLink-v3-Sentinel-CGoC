# 2026-05-23 — Project init + memory & wiki scaffold

## Context
- Resumed via `resume v3` after a fresh Claude Code session start.
- `ref/00-RESUME-POINT.md` confirmed: pre-Phase-0, no code, gates open.
- Noel requested: update `AGENTS.md`, scaffold the memory system (per `memory-system-architect.md`), scaffold the LLM wiki (per `llm-wiki.md`), include mem0.

## Decisions
- **mem0: dropped.** Memsearch (Level 3) already covers semantic recall; mem0 would be duplicative as a Claude-Code memory layer. Sentinel runtime memory for agents in Phase 10 can revisit this when stack is locked.
- **Wiki seeding:** seed `wiki/project/` from the resume point + meeting notes; keep `runbooks/`, `manuals/`, `decisions/` as growable stubs.
- **File extensions:** `.md` over `.mmd` (the spec called for `.mmd` but `.md` is universal; Noel approved either).
- **Git:** init local, add remote `github.com/noelferrer-01/TaoLink-v3-Sentinel-CGoC`, push initial commit.

## Surfaced contradictions (logged in wiki)
- **Recruitment vs Deployment ownership** — Sentinel architecture (per `/ref/`) splits Recruitment (Phase 5) from Deployment (Phase 2). Commander Group meeting notes §1D say **only Recruitment has authority to transfer guards**. These are structurally incompatible. Decision recorded as `wiki/decisions/0001` — pending questionnaire validation with Commander Group department heads.

## Meta — caveats on existing /ref/ material
- `ref/sentinel-docs/commander-group-meeting-notes.md` is a **Claude-generated summary**, not Noel's raw notes. Possible gaps/inaccuracies/inferred details.
- Figma flowchart (Commander Group HRIS Full Suite Workflow) was also Claude-derived; treat as starting point, not binding spec.
- Full meeting transcription available on request — to be dropped as `ref/sentinel-docs/*.txt`. **Exclude email-system portions** (separate completed project).

## Open question raised this session
- **Docker vs MAMP** (Noel) — relevant when stack is locked. Quick answer: Docker is the modern default for reproducible local dev across any stack (Node/Python/PHP/etc.); MAMP is a legacy LAMP/MAMP-stack convenience for PHP+MySQL only. For Sentinel (whatever stack), Docker Compose is the right base. Filing as a sub-decision under the stack call.

## Next action
- Phase 0 still gated. Per `ref/00-RESUME-POINT.md` §8, the menu is:
  - (a) Send the client questionnaire to Commander Group
  - (b) Lock the stack call (TS vs Python)
  - (c) Write Sentinel v2 architecture doc
  - (d) Phase 0 implementation plan (after a/b/c)
  - (e) Demo runbook for v1
  - (f) Something else

## Artifacts touched
- Modified: `AGENTS.md` (project header added; existing content preserved)
- Created: `memory/` tree (index, general, README, 4 domain files, today's daily log)
- Created: `wiki/` tree (index, log, conventions, 3 project pages, 1 decision, runbook+manual stubs)
- Created: `.claude/settings.json` + `.claude/scripts/inject_memory_index.sh`
- Created: `.gitignore`, `.env.example`
- Initialized: git repo on `main` branch, remote `github.com/noelferrer-01/TaoLink-v3-Sentinel-CGoC` configured. **First commit local; push pending** — Claude's git creds resolve to `noelferrer` (HTTPS) and no SSH key for `noelferrer-01`. Noel must push from his terminal: `git push -u origin main`.
