# 2026-06-12 — Slice 4 finished, shipped, merged + HR/compliance roadmap

Resumed from the 2026-06-11 checkpoint (Slice 4 engine T1–T8 already built in the prior session). This session took Slice 4 from T9 to merged-on-main, then did a domain review that became the Slice 5+ roadmap.

## Slice 4 (Billing & SOA) — completed end to end
- **T9 — Billing-area UI** (subagent implementer → spec + quality review). `/billing` list + Generate SOA control + period-level Unattributed panel (Re-attach) + `/billing/[invoiceId]` printable per-guard SOA + status-gated Finalize/Mark-paid + sidebar nav. Issuer = **Commander Group** (not SistemaHub); Due = periodEnd + paymentTermsDays.
- **T10 — verify.** Brought the env up on the **Docker Postgres (`:5433`)** — found 0027 wasn't applied there (it had gone to native `:5432` while Docker was down), applied it to dev + test. **Full suite 418 → 420 green.** Playwright walk (login → set 2 client rates → Generate SOA, 6 guards × 16 days × ₱780 = ₱82,368 → Finalize `2026-0001` → Mark paid → Unattributed surface + Re-attach), screenshots Read, audit trail confirmed. Updated billing/dtr/payroll READMEs + wrote the done-sweep.
- **Ship via `/go`** — test → `/simplify` (extracted shared `app/(admin)/billing/_format.ts`; folded action runner) → **PR #4** → cloud multi-agent **code-review**. Review flagged one ≥80 bug (paidAt rendered the UTC day, off-by-one for 00:00–07:59 PHT) + five sub-threshold items; **all fixed in `caec279`** (Manila-time `formatPhInstant`, shared `plainMessage` prefix-strip, `reattributeDtrDay` try/catch, status-aware finalize/markPaid +2 tests, exported type, README/JSDoc).
- **Merged** PR #4 → `main` (merge commit `a334007`), tagged **`slice-4-done`**, deleted the `slice-4-billing` branch. The money cycle (pay guard → bill client) is now closed and on `main`.

## HR-ops & PH-compliance review → the Slice 5+ roadmap
Did a review from a guard-agency HR head's seat over everything through Slice 4. Saved as a standing guide: [`../../wiki/reviews/2026-06-12-hr-operations-compliance-review.md`](../../wiki/reviews/2026-06-12-hr-operations-compliance-review.md) (committed `b60bc17`, wired into wiki + memory indexes). Key output = a **ranked gap register** with the DOLE/legal "why" and a recommended slice sequence. See [compliance.md](../domains/compliance.md#v3-realized-state--gap-roadmap-2026-06-12).

## Next target (decided)
**Slice 5 = wage-stack engine** (complete the guard payslip: NSD, holiday 200%/130%, rest-day premium, 13th-month, SIL + a holiday-calendar table). Unblocked by client questions (rates are statutory, in `ref/`). Bundle the **floating/off-detail 6-month clock** (small, highest legal payoff). **RBAC** is the must-do guardrail before real guard data enters.

## Stable facts promoted
- Sentinel is now in **active vertical-slice development; Slices 0–4 shipped + merged** (was "Pre-Phase-0"). → [general.md](../general.md) Status rewritten; [architecture.md](../domains/architecture.md) reframed (the old phase-order is superseded by realized slices).
- v3 **realized payroll scope = basic + 4 statutory deductions only**; the guard wage stack is deferred. → [compliance.md](../domains/compliance.md).
